//https://codeforces.com/problemset/problem/637/B
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

   int n;cin>>n;
   stack<string>st;
   map<string,int>m;
    for (int i = 0; i < n; ++i) {
        string ss;cin>>ss;
        st.push(ss);
    }

    while(!st.empty()){
        if(!m[st.top()]){
            m[st.top()]++;
            cout<<st.top()<<"\n";
        }
        st.pop();

    }
    return 0;
}
