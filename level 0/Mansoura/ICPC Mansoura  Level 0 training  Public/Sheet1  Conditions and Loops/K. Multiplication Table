#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;cin>>t;
    for (int i = 1; i <= 10 ; ++i) {
        cout<<i<<" x "<<t<<" = "<<i*t<<"\n";
    }
    return 0;
}
