#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    double  a,b,c;cin>>a>>b>>c;
    double sum=a*2+b*3+c*5;
    cout<<fixed<<setprecision(1)<<"MEDIA = "<<sum/10;
    return 0;
}
